networks:
  freemarket-network:
    external: true
    name: free-market-backend-_freemarket-network

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: free-market-frontend
    ports:
      - "4200:80"
    restart: unless-stopped
    networks:
      - freemarket-network