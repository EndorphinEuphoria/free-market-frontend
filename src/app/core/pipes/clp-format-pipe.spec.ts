import { ClpFormatPipe } from './clp-format-pipe';

describe('ClpFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ClpFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
